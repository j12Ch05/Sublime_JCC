{
    "shell_cmd": "g++ \"${file}\" -o \"${file_path}/${file_base_name}\" -lfreeglut -lglew32 -lopengl32 -lglu32",
    "file_regex": "^(..[^:]*):([0-9]+):?([0-9]+)?:? (.*)$",
    "working_dir": "${file_path}",
    "selector": "source.c, source.cpp",
    "variants":
    [
        {
            "name": "Run",
            "shell_cmd": "g++ \"${file}\" -o \"${file_path}/${file_base_name}\" -lfreeglut -lglew32 -lopengl32 -lglu32 && \"${file_path}/${file_base_name}\""
        }
    ]
}